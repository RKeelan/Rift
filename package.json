{
  "name": "rift",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["shared", "server", "client", "cli"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"bun run --filter server dev\" \"bun run --filter client dev\"",
    "prod": "bun run build && bun run --filter server dev",
    "tailscale": "tailscale serve --bg 13000",
    "build": "bun run --filter shared build && bun run --filter server build && bun run --filter client build && bun run --filter cli build",
    "lint": "biome lint .",
    "lint:pkg": "npmPkgJsonLint . shared server client cli",
    "format:check": "biome format .",
    "test": "bun test --cwd server && bun test --cwd client && bun test --cwd cli"
  },
  "overrides": {
    "@codemirror/state": "6.6.0",
    "@codemirror/view": "6.40.0",
    "@codemirror/language": "6.12.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "concurrently": "9.2.1",
    "npm-package-json-lint": "10.2.1",
    "typescript": "6.0.3"
  }
}
